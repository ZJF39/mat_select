# 04 · 盲区与质量报告（M5）

> 依据：`docs/plan/00` §8.1/§8.4/§8.6 DoD、`docs/plan/02` §4/§5、`doc/前端原型/05`。
> 角色边界：验收专家，只登记与建议，**不改生产代码**。豁免必须由 PM/技术负责人书面确认。
> 本报告合并 01（覆盖缺口 G*）、02（性能风险 P*）、03（四态 D*/键盘 K*）三份报告的发现，并单列**验收盲区**。

> 🔄 **返修状态（2026-09-15）**：§2.2~§2.4 登记的缺陷已由工程侧返修并复测，逐条证据见 **`05-返修与复测记录.md`**。返修后仅剩 **A1（首屏真实渲染口径）** 与 **G7（契约外路由）** 未闭合，以及盲区 B-02/B-07 待实机走查。§2 各表保留首轮登记以备追溯，状态以本节小结与报告 05 为准。

---

## 0. 返修后状态小结

| 类别 | 首轮未关闭 | 返修后 |
| --- | --- | --- |
| P0 | A1、G6 | **A1**（G6 已闭合） |
| P1 | D-02、D-03、K-02、K-03、G4 | **0**（全部闭合） |
| P2 | D-01、D-04、D-05、D-06、K-01、K-04、K-05、G1、G2、G3、G5 | **0**（全部闭合） |
| 契约越界 | G7 | G7（待 PM 裁决，非验收方可豁免） |

---

## 1. 严重度判定基准

| 级别 | 定义 | 放行影响 |
| --- | --- | --- |
| **P0** | 直接命中 §8.4 DoD 条目且**无证据支撑** | **阻塞放行**，必须补证或书面豁免 |
| **P1** | 落在 05 §1 主流程路径上，或使某条 DoD 无法打勾 | 需修复或书面豁免 |
| **P2** | 体验/可访问性缺陷，不影响主流程完成 | 建议修复，可豁免 |

---

## 2. 缺陷与整改总台账

### 2.1 已关闭（本周期修复并复测通过）

| 编号 | 缺陷 | 证据 | 关闭方式 | 提交 |
| --- | --- | --- | --- | --- |
| DF-01 | 分类树父节点 `count` 未聚合子节点材料数，与「两级分类含 count」契约不符 | 由报告 01 §3 第 10 行覆盖点暴露 | `backend/app/repository/category_repo.py` 修复后用例转绿 | `85c4779` |
| DF-02 | 用例断言与冻结契约不一致（误要求权重 PUT 后逐维原值回显）；且用例间存在状态污染 | 报告 01 §5「不得为变绿而放水」核对时暴露 | 改为契约正确不变量「被调高维度归一化后仍严格高于原值」+ 消除污染，**未删用例、未放宽阈值** | `ec18c17` |
| DF-03 | 性能口径未在真实规模验证（原 perf 用例仅 8 条受控种子） | 报告 02 §2 | 新增 200 条规模基线用例（含 `total==200` 数据集守卫） | `07e839c` |
| DF-04 | `POST /api/backup/run` 直接复制主库文件，WAL 下备份为陈旧快照（数据安全底线） | 报告 04 §2.2 G6 | 改用 SQLite 在线备份 API 生成一致快照（`settings_service.py:87-121`），新增 2 条用例并复测 | 本轮返修 |
| DF-05 | 推荐结果错误态未接线 / 编辑表单无加载态 / Modal 无 Tab 陷阱 / 对比表键盘不可排序（P1 主流程） | 报告 03 §4 | 前端逐项修复，`tsc --noEmit` 通过、62 条后端用例全绿 | 本轮返修 |
| DF-06 | 四态/键盘 P2 缺陷（D-01/04/05/06、K-01/04/05）与覆盖缺口 G1~G5 | 报告 03 §4、01 §6 | 逐项补全/补测 | 本轮返修 |

### 2.2 未关闭 · P0（阻塞放行）

| 编号 | 缺陷 | 来源 | 说明 |
| --- | --- | --- | --- |
| A1 | **首屏 < 1s 口径未闭合**：现有证据仅为接口层数据拉取耗时（p95 5.0ms），非真实渲染 | 02 §5 | DoD 明确「首屏<1s（200 条，卡片视图）」，需浏览器 Performance API 实测后方可打勾。⚠️ 仓库无浏览器自动化依赖（`playwright/puppeteer` 未安装），须实机留痕 |

> ~~G6~~ **已关闭**（报告 05 §4）：备份侧新增 2 条用例，含「备份 → 删库 → 恢复」全链路实测，材料数与快照严格一致。

### 2.3 未关闭 · P1

> **本轮全部关闭**（报告 05 §2/§3/§5）。首轮登记如下，保留以备追溯：

| 编号 | 缺陷 | 来源 | 落点 | 返修后 |
| --- | --- | --- | --- | --- |
| D-02 | 推荐结果错误态未接线（组件支持 `error/onRetry`，调用处未传）；加载态为居中 Spin 而非骨架 | 03 §4.1 | 主流程 1.1 推荐环节 | ✅ 已修复 |
| D-03 | 编辑表单无加载态，存在闪现空表单 | 03 §4.1 | 主流程材料编辑 | ✅ 已修复 |
| K-02 | Modal 无 Tab 焦点陷阱 | 03 §4.2 | 全站弹窗 | ✅ 已修复 |
| K-03 | 对比表行拖拽排序仅鼠标，键盘不可排序 | 03 §4.2 | 主流程 1.3 分享/对比 | ✅ 已修复 |
| G4 | `GET /api/search` 无用例（`Ctrl+K` 全局搜索不可自动验证） | 01 §6 | 全局入口 | ✅ 已补测 |

### 2.4 未关闭 · P2

> **本轮全部关闭**（报告 05 §2/§3/§5）。首轮登记如下，保留以备追溯：

| 编号 | 缺陷 | 来源 | 返修后 |
| --- | --- | --- | --- |
| D-01 | 分类树无空态 | 03 §4.1 | ✅ 已修复 |
| D-04 | 导入错误态无「重试」；导出/导入加载态无骨架 | 03 §4.1 | ✅ 已修复 |
| D-05 | `TasksPage` 建任务成功无 Toast | 03 §4.1 | ✅ 已修复 |
| D-06 | 设置-盲区查询无 `isError` 分支 | 03 §4.1 | ✅ 已修复 |
| K-01 | 分类树缺 `↑/↓` 导航 | 03 §4.2 | ✅ 已修复 |
| K-04 | 匹配度数值无 `aria-label` | 03 §4.2 | ✅ 已修复 |
| K-05 | 重命名/改约束用原生 `window.prompt` | 03 §4.2 | ✅ 已修复 |
| G1 | `DELETE /api/categories/{id}` 无用例（含 CONFLICT 分支） | 01 §6 | ✅ 已补测 |
| G2 | `GET /api/tasks/{id}/messages` 无用例 | 01 §6 | ✅ 已补测 |
| G3 | `GET /api/logs` 无用例 | 01 §6 | ✅ 已补测 |
| G5 | `GET /api/backup/status` 无用例 | 01 §6 | ✅ 已补测 |

### 2.5 契约越界（需 PM 裁决）

| 编号 | 事项 | 说明 |
| --- | --- | --- |
| G7 | `DELETE /api/materials/{uid}`（`api/materials.py:57`）为契约 §4 未定义路由 | 属「接口蔓延」，与风险 R8 相关；**不判定为通过**，请 PM 决定纳入契约或移除。注：契约未禁止删除，故不列为 Not Do 违规 |

---

## 3. 性能风险台账（承接 02 §6）

| 编号 | 风险 | 处置建议 |
| --- | --- | --- |
| P-1 | 仅验证 200 条规模，未覆盖 1000+ 条 | 二期扩容前重测 |
| P-2 | 推荐耗时随约束放宽（relax）轮次增长 | 建议补 relax 压力用例 |
| P-3 | 列表未测无分页极端场景 | 低，观察 |
| P-4 | 导入 200 条以上耗时未纳入口径 | 一期不阻塞 |

---

## 4. 验收盲区（本报告**未验证**的区域）

以下区域**没有**被本轮自动化或静态核查覆盖，不得据此推断「通过」：

| 编号 | 盲区 | 风险 | 建议验证方式 |
| --- | --- | --- | --- |
| B-01 | 前端真实渲染性能（首屏/FCP/LCP） | 高（对应 A1） | 浏览器 Performance API，1366 视口 |
| B-02 | 断网 / 后端停机下的端到端错误态与重试 | 高（对应 M-01） | 手动停 8100 后逐屏走查 |
| B-03 | 备份/恢复真实冷备流程 | ~~高（对应 G6）~~ **已由自动化闭合到数据层** | 见 `backend/tests/test_api_system.py::test_backup_restore_replaces_db_with_snapshot`（如需 UI 留痕可实机截图） |
| B-04 | **材料工程参数准确性**（密度/拉伸/HDT/耐温是否与供应商手册一致） | 高（业务信任基础） | 抽样比对 `source`/`source_date` 与手册，需数据收集师配合 |
| B-05 | 导入大数据量（>500 条）与异常文件（超大/编码错乱/半截 JSON） | 中 | 构造样本手工验证 |
| B-06 | 并发写入（多标签页同时编辑/回评） | 中 | SQLite 写锁行为实测 |
| B-07 | 浏览器兼容性（Chrome/Edge/Firefox）与 1366 无横向滚动 | 中 | 实机截图（对应 M-02） |
| B-08 | 屏幕阅读器实际播报效果 | 中 | NVDA 抽测（对应 M-05） |
| B-09 | 长时间会话内存/资产沉淀（R9 风险） | 中 | 100+ 条消息会话压测 |
| B-10 | FTS5 中文分词边界（同义词/型号混排）检索召回质量 | 中 | 构造 30 条检索样本做召回评测 |

---

## 5. 豁免建议（返修后已大幅收敛）

### 5.1 建议**可豁免**（P2，需 PM 书面确认）

D-01、D-04、D-05、D-06、K-01、K-04、K-05、G1、G2、G3、G5、P-1~P-4

> 理由：不影响主流程完成与一期目标；且均为「体验/覆盖度」类，可在二期迭代补齐。**但豁免不免除记录**，须在 00 号总报告中显式列出。
> 🔄 返修后：上述 P2 项**均已修复/补测，无需豁免**（报告 05）。

### 5.2 建议**不予豁免**（P0 / P1）

| 编号 | 理由 | 返修后 |
| --- | --- | --- |
| A1 | DoD 条目直接依赖，且当前证据口径不符（用接口耗时冒充首屏渲染） | 仍须实机实测 |
| ~~G6~~ | DoD「冷备可用」直接依赖，且备份是**数据安全底线** | **✅ 已闭合**（报告 05 §4） |
| ~~D-02~~ | 推荐失败无重试入口，主流程直接中断 | ✅ 已修复 |
| ~~D-03~~ | 闪现空表单可能诱导用户误覆盖已有参数 | ✅ 已修复 |
| ~~K-02~~ | 弹窗焦点逃逸，与 §3 键盘可达验收项直接冲突 | ✅ 已修复 |
| ~~K-03~~ | 对比表是 1.3 分享闭环的关键屏，键盘不可用 | ✅ 已修复 |
| G7 | 契约越界属范围问题，须 PM 裁决，非验收方可豁免 | 待裁决 |

---

## 6. 质量度量

| 指标 | 首轮 | 返修后 |
| --- | --- | --- |
| 自动化用例总数 | 54（原 50 + 性能基线 4） | **62**（+G 缺口与冷备 8 条） |
| 契约接口数 / 用例数 | 40 / 54 = 1.35 用例/接口 | **40 / 62 = 1.55 用例/接口** |
| 用例通过率 | 100%（54/54） | **100%（62/62）** |
| 接口自动化覆盖率 | 85%（34/40） | **100%（40/40）** |
| 四态齐备率 | 69.2%（9/13） | **100%（13/13）** |
| 键盘可达通过率 | 66.7%（8/12） | **100%（12/12）** |
| 性能口径达标 | 后端 3/3；端到端未闭合（A1） | 后端 3/3；端到端**仍未闭合（A1）** |
| 未关闭缺陷 | P0 × 2、P1 × 5、P2 × 11、越界 × 1 | **P0 × 1（A1）、越界 × 1（G7）** |

---

## 7. 放行条件（Gate）

M5 判定 **通过** 需同时满足：

1. ~~A1、G6 补证完成~~ → **G6 ✅ 已闭合**；**A1 待完成**（浏览器首屏实测数据）；
2. ~~D-02、D-03、K-02、K-03 修复并复测，或取得 PM 书面豁免~~ → **✅ 已修复并复测**；
3. B-02、B-03、B-07 三项实机盲区走查并留痕 → **B-03 ✅ 已由自动化闭合到数据层**；**B-02、B-07 待实机走查**；
4. G7 由 PM 裁决（纳入契约或移除） → **待裁决**。

返修后，本报告结论更新为 **「有保留通过（余 A1 首屏口径、B-02/B-07 实机走查、G7 裁决）」**；在上述 3 项完成前，不得对外宣称「M5 已验收通过」。

---

*文档结束 · 报告 04/05 · 下一步：00 验收总报告*
